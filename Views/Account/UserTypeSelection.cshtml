@{
    ViewData["Title"] = "Welcome Page";
    Layout = null; // Set the Layout property to null

}

<!DOCTYPE html>
<html>
<head>
    <style>
        /* CSS styles for the UserTypeSelection view */
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            text-align: center;
        }

        .container {
            margin: 20px;
        }

        h1 {
            color: #333;
            font-size: 24px;
        }

        p {
            font-size: 18px;
            color: #555;
        }

        .user-type-options {
            display: flex;
            justify-content: center;
        }

        .user-type-option {
            margin: 10px;
            font-size: 20px;
        }

        .continue-button {
            background-color: navy;
            color: white;
            padding: 10px 20px;
            font-size: 20px;
            border: none;
            cursor: pointer;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Online Election Portal</h1>
        <br /> <br />
        <p>Which of the following roles do you belong to?</p>
        <form method="post" action="/Account/SelectUserType">
            <div class="user-type-options">
                <label class="user-type-option">
                    <input type="radio" name="userType" value="Admin" /> Admin
                </label>
                <label class="user-type-option">
                    <input type="radio" name="userType" value="Candidate" /> Candidate
                </label>
                <label class="user-type-option">
                    <input type="radio" name="userType" value="Voter" /> Voter
                </label>
            </div>
            <br />
            <button class="continue-button" type="submit">Continue</button>
        </form>
    </div>
</body>
</html>
